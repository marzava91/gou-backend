/* =========================
   ENUMS (ERP CORE)
========================= */

// 10_identity_access

enum UserStatus {
  ACTIVE
  SUSPENDED
  DEACTIVATED
  ANONYMIZED
}

enum UserContactChangeType {
  PRIMARY_EMAIL
  PRIMARY_PHONE
}

enum UserContactChangeStatus {
  PENDING
  VERIFIED
  CONSUMED
  EXPIRED
  CANCELLED
} 

enum AuthProvider {
  FIREBASE
  PASSWORD
  EMAIL_CODE
  PHONE_CODE
  GOOGLE
  APPLE
}

enum AuthSessionStatus {
  ISSUED
  ACTIVE
  REFRESHED
  REVOKED
  EXPIRED
  LOGGED_OUT
}

enum AuthVerificationChallengePurpose {
  VERIFY_EMAIL
  VERIFY_PHONE
  PASSWORD_RESET
  LOGIN
  STEP_UP
}

enum AuthVerificationChallengeStatus {
  ISSUED
  VALIDATED
  FAILED
  EXPIRED
  CONSUMED
}

enum MembershipScopeType {
  TENANT
  STORE
}

enum MembershipStatus {
  PENDING
  ACTIVE
  SUSPENDED
  REVOKED
  EXPIRED
}

/// MVP note:
/// OperationalSurface currently models only PARTNERS_WEB because
/// ActiveMembershipContext is explicitly supported only for that surface.
/// Other administrative or transaccional surfaces must be added only after
/// confirming they consume Memberships with the same context semantics.
enum OperationalSurface {
  PARTNERS_WEB
}

enum RoleScopeType {
  TENANT
  STORE
}

enum RoleAssignmentStatus {
  ACTIVE
  REVOKED
}

enum GrantEffect {
  ALLOW
  DENY
}

enum GrantTargetType {
  CAPABILITY
  RESOURCE_ACTION
}

enum GrantStatus {
  PROPOSED
  ACTIVE
  EXPIRED
  REVOKED
}