 * TODO(testing-next):
 * - add happy-path HTTP coverage for admin commands:
 *   - POST /v1/users
 *   - GET /v1/users/:id
 * - add HTTP coverage for self-service contact change flows:
 *   - POST /me/primary-email-change/request
 *   - POST /me/primary-email-change/confirm
 *   - POST /me/primary-phone-change/request
 *   - POST /me/primary-phone-change/confirm
 * - add one higher-level integration suite with real module wiring + test DB
 